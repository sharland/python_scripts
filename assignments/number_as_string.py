#! /usr/local/bin/python3

#defines a variable called number and assigns it a value of 2 but as a string instead of an integer
number = str(2)

#prints a string but with in addition the variable number 'added' to number - what do you think will happen?
print ("2 + 2 = ", number + number)